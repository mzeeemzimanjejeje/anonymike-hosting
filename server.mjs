// Root-level entry — satisfies Pterodactyl egg install verification.
// The compiled server bundle lives at artifacts/api-server/server.mjs
// Startup command: node index.js  (see index.js at repo root)
import './artifacts/api-server/index.js';
